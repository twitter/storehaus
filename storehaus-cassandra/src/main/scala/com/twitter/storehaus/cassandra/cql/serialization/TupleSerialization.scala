package com.twitter.storehaus.cassandra.cql.serialization

import shapeless._
import com.websudos.phantom.CassandraPrimitive

object TupleSerialization {

  
  // implementation of values that are Tuples and fetch more columns, if they are fetched
  // uses abstraction over arity
}